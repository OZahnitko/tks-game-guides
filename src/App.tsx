import { Map } from "@components";

import { AppContainer } from "./Styles";

const App = () => {
  return (
    <AppContainer.RootWrapper>
      <Map />
    </AppContainer.RootWrapper>
  );
};

export default App;
